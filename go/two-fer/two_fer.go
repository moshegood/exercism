// Package twofer package comment here
package twofer

// ShareWith returns stuff.
func ShareWith(name string) string {
	if len(name) == 0 {
		name = "you"
	}
	return "One for " + name + ", one for me."
}
